"use client";
import { NextPage } from "next";
import { GeolocalizacaoView } from "src/views/cliente/novoEndereco/geolocalizacao";

const GeolocalizacaoPage: NextPage = () => {
  return <GeolocalizacaoView />;
};

export default GeolocalizacaoPage;
